{
  "name": "@community/onnx-embedder",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/server.ts",
  "scripts": {
    "dev": "bun run --hot src/server.ts",
    "build": "bun build --compile --minify --sourcemap --bytecode ./src/server.ts --outfile server",
    "start": "./server"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.7",
    "@xenova/transformers": "^2.17.2",
    "hono": "catalog:",
    "onnxruntime-node": "^1.20.1",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/bun": "catalog:",
    "typescript": "catalog:"
  }
}
