{
  "name": "@community/onnx-pron",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/server.ts",
  "scripts": {
    "dev": "bun run --hot src/server.ts",
    "build": "bun build --compile --minify --sourcemap --bytecode ./src/server.ts --outfile server",
    "start": "./server"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.7",
    "hono": "catalog:",
    "onnxruntime-node": "^1.20.1",
    "wav": "^1.0.2",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/bun": "catalog:",
    "@types/wav": "^1.0.4",
    "typescript": "catalog:"
  }
}
