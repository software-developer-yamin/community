# Copy to .env (gitignored) and adjust. docker-compose reads these automatically.
# Optional: override the YAML `keys:` block without editing livekit.yaml.
# Format: "key1: secret1, key2: secret2"
LIVEKIT_KEYS=devkey: devsecret_replace_with_long_random_string_for_prod
